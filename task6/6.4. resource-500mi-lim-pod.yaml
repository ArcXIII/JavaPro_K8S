apiVersion: v1
kind: Pod
metadata:
  name: resource-500mi-lim-pod
  namespace: quota-lab
spec:
  containers:
    - name: nginx
      image: nginx:alpine3.22-slim
      resources:
        limits:
          memory: 500Mi